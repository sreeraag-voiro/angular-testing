# AngularTesting

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 13.2.5.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Generating code-coverage reports

Run `ng test --code-coverage` to execute the unit tests and generate code-coverage reports using Istanbul. The generated code-coverage reports will reside in the `coverage` folder of the repository.  

## Further help

To see why we are writing unit tests, head [here](https://docs.google.com/presentation/d/15x6I8Z5h5wPSp3wOMCRIOxUat8bYIKKji1u3OuAKk7s/edit?usp=sharing)